import { StyleSheet } from "react-native";

export const LogoContainerStyles = StyleSheet.create({
  container: {
    position: "relative",
    justifyContent: "center",
    alignItems: "center",
    marginRight: 20,
  },
  logo: {
    width: 30,
    height: 30,
    borderRadius: 50,
  },
});
